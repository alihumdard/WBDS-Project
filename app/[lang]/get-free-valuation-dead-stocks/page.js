import ContactPage from "../contact/page";

export const metadata = {
    title: "Get Free Valuation - We Buy Dead Stocks",
    description: "Request a free valuation for dead stock, surplus inventory, scrap, and non-moving stock.",
};

export default function GetFreeValuationPage() {
    return <ContactPage />;
}
